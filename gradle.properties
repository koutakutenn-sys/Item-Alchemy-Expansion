# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=26.2
loader_version=0.19.5
loom_version=1.17.13

# Mod Properties
mod_version=1.2.0+26.2.port.1
maven_group=itemalchemy.expansion
archives_base_name=itemalchemy-expansion

# Dependencies
fabric_version=0.161.0+26.2

# --- Addon target (Item Alchemy) + its dependency (mcpitanlib) ---
# NOTE: build.gradle currently consumes prebuilt jars from libs/ (not committed, see PORT_26.2.md),
# so these coordinates are NOT used by the build today. They are kept in sync with the verified
# 26.2 environment for when the build is switched back to Maven dependencies.
# Verifying them is currently blocked: maven.pitan76.net answers 403 (Cloudflare) from this network.
itemalchemy_version=1.3.9
mcpitanlib_version=4.0.7-fix.1

# --- Optional config UI ---
cloth_config_version=26.2.155
modmenu_version=20.0.2
